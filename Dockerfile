# Используйте базовый образ с нужной версией JDK
FROM openjdk:22-jdk-slim

# Установите рабочую директорию
WORKDIR /app

# Скопируйте ваш JAR-файл в контейнер
# Предполагается, что у вас есть собранный JAR (например, my-app.jar)
//COPY target/User-service.jar /app/User-service.jar

# Укажите порт, который будет слушать ваше приложение (если применимо)
EXPOSE 8080

# Команда для запуска вашего приложения
CMD ["java", "-jar", "User-service.jar"]